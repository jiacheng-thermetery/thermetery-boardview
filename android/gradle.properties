# JVM args for the Gradle daemon / build JVM.
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8

# AndroidX is required by AGP 8.x and our androidx.* dependencies.
android.useAndroidX=true

# Smaller R classes; we only use our own resources.
android.nonTransitiveRClass=true
